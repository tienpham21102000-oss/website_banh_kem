{
  "name": "website-banh-kem-root",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && cd backend && npm install pg",
    "build:frontend": "npm run build --prefix frontend",
    "start": "cd backend && npm start",
    "deploy": "npm run install:all && npm run build:frontend && npm run start"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
